---

# metadata.name
source:
  version: builtin
  kind: HelmChartInflationGenerator
  name: redis-ha

  fieldPath: metadata.name

targets:

  # PrefixSuffixTransformer
  - select:
      version: builtin
      kind: PrefixSuffixTransformer
      name: generator-prefix
    fieldPaths:
      - prefix
